{
  "name": "conectados-factura-lambda",
  "version": "1.0.0",
  "description": "Lambda functions for Conectados Factura+",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "deploy": "npm run build && aws lambda update-function-code --function-name",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.456.0",
    "@aws-sdk/lib-dynamodb": "^3.456.0",
    "@aws-sdk/client-s3": "^3.456.0",
    "@aws-sdk/client-secretsmanager": "^3.456.0",
    "@aws-sdk/cognito-identity-provider": "^3.456.0",
    "@aws-sdk/client-textract": "^3.456.0",
    "uuid": "^9.0.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.8.7",
    "@types/uuid": "^9.0.6",
    "typescript": "^5.2.2",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.6",
    "ts-jest": "^29.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
