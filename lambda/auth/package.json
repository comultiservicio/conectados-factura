{
  "name": "conectados-factura-auth",
  "version": "1.0.0",
  "description": "Auth Lambda function for Conectados Factura+",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "aws-sdk": "^2.1484.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^18.19.0"
  }
}
